// TODO(ads-disabled-018): entire ad layer paused — restore by removing this
// header and the closing block comment below. See specs/018-disable-ads-isolate-downloads/.
/*
/// Single context-free premium signal for the ad layer (research R10).
///
/// Synced from `SubscriptionCubit` at the app root
/// (`shouldShowAds = !isPremium`). Every full-screen ad manager checks this
/// before showing; the banner widget additionally short-circuits via bloc.
/// Premium users must never see ads (FR-018, constitution VIII).
class AdGatekeeper {
  /// Defaults to `true` (guest). Set to `!isPremium` whenever the
  /// subscription state changes.
  bool shouldShowAds = true;
}
*/
